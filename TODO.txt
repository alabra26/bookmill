# 03/17/2025
Build out the following capabilities:
- add cells to a notebook object from a configuration object (cell type, cell source, cell tags), and execute
- perform the above, and validate outputs
	- does this make sense to do outside of the code? Or should code check cells be added to the notebook?
- perform the above, and determine namespace changes (what new variables are defined?)
- determine variable dependencies for a code block
- trim code check cells and namespace determination cells
	- likely this will be done with cell tags, but is there an easier way?

# 03/18/2025
:check: Revert commits to main and push all commits to a feature branch child of dev

# 03/20/2025
- pytest almost working, but requires bookmill.src.utils import path in tests,
  while in production we require src.utils import path
- find a better solution to the tmp save path for pm.execute_notebook
- remove all notebook modification side-effects (should not add cells inplace
  or add cell outputs after execution inplace)